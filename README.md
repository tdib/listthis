# Benji's React Template

## Setup

1. Run `yarn` to install dependencies
2. Run `yarn start` to start the development server
3. Run `yarn build` to build a production version of the app in the `dist` folder
